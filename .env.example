# Ngrok public base URL from Google Colab (can include or omit /v1)
HOST_BASE_URL="https://your-ngrok-subdomain.ngrok-free.dev/v1"

# Model identifier matching the vLLM instance
HOST_MODEL="Qwen/Qwen2.5-7B-Instruct-GPTQ-Int4"

# API Key (optional for default vLLM setups, leave as EMPTY or custom token)
HOST_API_KEY="EMPTY"